---
import Layout from '../layouts/Layout.astro';
import Card from '../components/card/Card.astro';

import projects from '../data/projects.json';
---

<Layout title="Portfolio | Projects | Duc Anh Dennis Nguyen">
  <main>
    <div class="inner">
      <div class="content-area">
        <div id="projects" class="section-header">
          <h1 class="sr-only">This page contains an overview of my projects</h1>
          <h2>Projects</h2>
        </div>
        <p>Here is a small sample of projects I have worked on.</p>
        <p>
          Many of these projects were written with Vue.js, but I am also
          proficient with Web Components/Vanilla JavaScript and I'm also familiar with React and AngularJS.
        </p>
        <p>
          I am already looking forward to grow the list significantly in the
          near future!
        </p>
      </div>
      <ul role="list" class="teaser-card-list">
        {
          projects.map((project) => (
            <Card
              href={project.href}
              title={project.title}
              body={project.body}
              image={project.image}
            />
          ))
        }
      </ul>
    </div>
  </main>
</Layout>

<style lang="scss">
  .teaser-card-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
    gap: 2rem;
    padding: 0;
  }
</style>
